# Deploy gmail-chat to Cloudflare Pages on push to main when examples/gmail-chat changes.
# Required repo secrets: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID
name: Deploy gmail-chat

on:
  push:
    branches: [main]
    paths:
      - "examples/gmail-chat/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Install and build
        working-directory: examples/gmail-chat
        run: bun install --frozen-lockfile && bun run build
      - name: Deploy to Cloudflare Pages
        working-directory: examples/gmail-chat
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: bunx wrangler pages deploy dist --project-name gmail-chat
