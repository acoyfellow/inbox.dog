{
  "name": "@inboxdog/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for inbox.dog — Gmail OAuth in one tool call",
  "type": "module",
  "bin": {
    "inbox-dog-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "typecheck": "tsc --noEmit"
  },
  "files": ["build"],
  "keywords": ["mcp", "gmail", "oauth", "email", "inbox.dog", "model-context-protocol"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
